{
  "name": "transit-ledger-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "prisma db push --accept-data-loss && prisma generate && nodemon src/index.js",
    "start": "prisma migrate deploy && prisma generate && node src/index.js",
    "db:migrate": "npx prisma migrate dev",
    "db:push": "npx prisma db push",
    "db:seed": "prisma db push && prisma generate && node prisma/seed.js",
    "db:studio": "npx prisma studio",
    "db:reset-data": "node prisma/reset-data.js",
    "test:e2e": "node tests/e2e.js",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "nodemon": "^3.1.7",
    "prisma": "^5.22.0"
  }
}